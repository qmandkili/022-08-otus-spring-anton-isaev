#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#  #sql:
#  #  init:
#  #    mode: always
#  #    data-locations: testdata.sql
#  sql:
#    init:
#      data-locations: testdata.sql
#      mode: always
spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: always
      data-locations: testdata.sql

  jpa:
    generate-ddl: false
    #generate-ddl: true
    hibernate:
      ddl-auto: none
      #ddl-auto: create-drop

    #show-sql: true

logging:
  level:
    ROOT: ERROR